# 学习与执行日志

## 2026-07-29

### 今天新增

- 建立 Python 隔离环境和项目目录。
- 确认 NCBI PubMed E-utilities 可访问。
- 完成真实文献检索与 Streamlit 页面。
- 用目标论文的 PMID、DOI 和摘要完成页面验收。
- 增加结构化证据模型、透明规则提取、带引用回答和导出功能。
- 真实摘要成功提取样本量、剂量、途径、时长和机制。
- 自动测试由 4 条增加到 9 条。
- 建立 30 条定向评测，覆盖检索、引用、结构化字段、缺失值和冲突保留。
- 增加 NCBI 临时失败重试、请求次数与 LLM 零成本记录。
- 完成 Dockerfile、PRD、架构、演示、简历证据、面试讲解和项目复盘。
- 最终回归：15 条自动测试和 30 条定向评测通过，依赖检查无损坏。
- 增加中科院 2025 与 JCR 2026 双分区展示，按 ISSN 优先匹配。
- 分区信息同步进入文献列表、证据表、Markdown 和 CSV。
- 分区数据采用可替换 CSV；默认只提供演示子集，未匹配期刊不猜测。
- 双分区功能回归后自动测试增加到 18 条。
- 将静态分区子集升级为 LetPub 动态查询：按 ISSN 定位期刊，分别解析中文页的中科院 2025 年 3 月升级版和英文页的 WOS JIF 分区。
- 增加 7 天 JSON 缓存、同批期刊去重、最多 4 路并发、过期缓存及本地 CSV 回退。
- JIF 结果保留 WOS 分类、SCIE 收录集和名次；界面、证据表及导出继续同时展示两套标准。
- 新增 LetPub 搜索页、分区页、缓存与 503 回退测试，自动测试增加到 21 条；定向评测仍为 30/30。

### 已运行命令

```powershell
python --version
git --version
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m pytest
.\.venv\Scripts\python.exe -m streamlit run app.py
```

### 遇到的问题与修复

- 第一版默认检索词包含过多限定词，真实 PubMed 返回 0 条。
- 将默认词精简为 `quercetin Streptococcus agalactiae mastitis` 后返回 2 条，并命中目标论文。
- 这说明检索不是关键词越多越好；过度限定会降低召回率。
- 新模块加入后，运行中的 Streamlit 保留旧模块缓存并出现导入错误。
- 完整重启 Streamlit 后恢复，说明热重载不能替代所有依赖变更后的进程重启。
- 首次评测为 28/30；两条失败来自评测器把 `answer_contains` 错当成通用字段检查。
- 修复评测器分支顺序并增加回归测试后，定向评测为 30/30。
- LetPub 中文页的大类分区数字位于同一单元格的可见 `span` 中，不能假设“大类”和“分区”一定分行；解析器按实际结构拆分后，真实页面返回中科院大类 3 区和兽医学小类 3 区。
- LetPub 同页同时提供 JIF 与 JCI 分区；本项目固定读取 `Quartiles By JIF`，避免把两种指标混在一起。

### 当前理解重点

- API 请求：程序把检索词发送给 NCBI，NCBI 返回结构化的文献数据。
- PMID：PubMed 给每条文献分配的唯一编号，可用于追溯来源。
- 隔离环境：`.venv` 把本项目依赖与系统 Python 分开。
- 结构化提取：把摘要中明确报告的信息映射到固定字段，未报告内容保持为空。
- 引用回答：结论旁边必须保留 PMID、DOI 或来源原句，不能只给无来源总结。
- 测试与评测不同：测试检查代码行为，评测检查当前任务样本上的结果质量与边界。
- Dockerfile 描述可复现环境，但只有实际构建和运行后才能声称 Docker 验证通过。

### 孙奇需要亲自完成

- [ ] 启动 Streamlit 页面。
- [ ] 输入一个检索词并打开一条 PMID 链接。
- [ ] 用自己的话解释“检索词 → NCBI API → 文献列表”的数据流。

### 下一步唯一重点

由孙奇配置 GitHub、验证 Docker、录制演示视频并完成一次亲自修改。
