# VetResearch Workbench v0.2 产品需求文档

## 1. 产品定位

VetResearch Workbench 基于 VetEvidence AI v0.1，把科研问题、可核查文献、实验条件和 CSV 数据组织成一个可审计的科研决策闭环。

当前首个垂直场景只处理：

> 候选药物与抗生素对目标病原菌是否存在值得进一步验证的协同作用？

系统辅助科研证据整理和实验设计，不提供医疗或兽医诊断、处方或临床决策。

## 2. 产品演进

### VetEvidence AI v0.1：已验证技术底座

v0.1 已完成真实 PubMed 检索、期刊分区、摘要级结构化提取、带引用回答、Markdown/CSV 导出和定向评测。该版本证明了“检索—提取—引用—导出”的可追溯闭环。

### VetResearch Workbench v0.2：当前增量

v0.2 不再停留在“查论文”，而是把问题与假设、多个文献来源、实验条件、实验 CSV、冲突与空白、决策报告和人工复核串联起来，并保存全过程审计记录。

## 3. 用户问题

科研人员形成实验判断时通常需要在多个界面和文件之间手工完成：

- 把宽泛问题拆成可验证假设；
- 组合多轮检索并去除重复文献；
- 比较不同论文中的样本、剂量、时间、对照和指标；
- 识别结论冲突及未报告条件；
- 计算 FICI 或比较生长曲线；
- 把文献证据、实验数据、风险和下一步写成报告；
- 记录失败、重试和人工复核。

这些步骤一旦失去来源对应关系，就容易把摘要推断、合成演示数据或未经核查的导入记录误当成事实。

## 4. v0.2 目标

- 将一个协同作用科研问题拆成 2—4 条透明、可人工修改的假设；
- 生成并执行最多 3 轮可见 PubMed 检索式，按 PMID 去重；
- 支持 RIS、EndNote、RefWorks 导入及 DOI/标题年份去重；
- 统一展示 PubMed 与用户导入来源，不为非 PubMed 来源伪造 PMID；
- 建立固定字段实验条件矩阵，显式报告缺失值、方向冲突和证据空白；
- 对 FICI 与生长曲线 CSV 做逐行校验和描述性计算；
- 输出带证据、风险、空白、下一步和人工复核的科研决策报告；
- 持久化任务事件、工具调用、失败和复核结果，并支持凭完整运行 ID 恢复；
- 保持 v0.1 的检索、提取、引用、分区和评测能力可用。

## 5. 非目标

- 不自动抓取知网或绕过任何平台权限；
- 不读取未授权全文，不支持扫描 PDF/OCR；
- 不覆盖所有实验类型，首个闭环只支持 FICI 和生长曲线；
- 不自动进行显著性推断、模型选择或因果判断；
- 不把动物、体外或摘要级结果直接外推到临床；
- 不训练或微调大模型；
- 不在 v0.2 引入多 Agent 框架、FastAPI、向量数据库、账号权限或云端协作；
- 不把合成演示数据当作科研证据；
- 不允许未经过人工复核的报告成为最终决策。

## 6. 核心用户流程

1. 用户输入科研问题、研究对象、候选干预、联合药物和预设指标；
2. 系统用透明规则生成 2—4 条可检验假设，用户可修改；
3. 系统生成并执行最多 3 轮 PubMed 检索，以 PMID 去重；
4. 用户可额外导入 RIS、EndNote、RefWorks 题录，系统规范化并去重；
5. 系统将所有可见来源整理为实验条件矩阵，保留来源身份和缺失字段；
6. 系统识别摘要级一致性、显式方向冲突和实验条件空白；
7. 用户上传 FICI 或生长曲线 CSV，系统逐行校验并执行描述性分析；
8. 系统生成带来源、风险与下一步的决策报告；
9. 用户选择通过、要求修改或拒绝；
10. 系统把全过程写入 `.workbench/runs/<run_id>.json`，可在页面恢复。

## 7. 功能需求

| 编号 | 版本 | 需求 | 当前状态 |
|---|---|---|---|
| FR-01 | v0.1 | 检索真实 PubMed 数据 | 已完成 |
| FR-02 | v0.1 | 展示题录、摘要、PMID、DOI | 已完成 |
| FR-03 | v0.1 | 提取结构化证据且不补造缺失字段 | 已完成 |
| FR-04 | v0.1 | 回答保留 PMID、DOI 和来源原句 | 已完成 |
| FR-05 | v0.1 | 导出 Markdown 和 CSV | 已完成 |
| FR-06 | v0.1 | 空结果、网络和解析错误处理 | 已完成 |
| FR-07 | v0.1 | 定向评测和错误分类 | 已完成 |
| FR-08 | v0.1 | 展示中科院与 WOS JIF 分区 | 已完成 |
| FR-09 | v0.1 | LetPub 动态查询、缓存与本地回退 | 已完成 |
| FR-10 | v0.2 | 问题拆解为 2—4 条可编辑假设 | 已完成 |
| FR-11 | v0.2 | 最多 3 轮 PubMed 检索并按 PMID 去重 | 已完成 |
| FR-12 | v0.2 | 导入 RIS、EndNote、RefWorks 并去重 | 已完成 |
| FR-13 | v0.2 | 统一实验条件矩阵且非 PubMed 来源不伪造 PMID | 已完成 |
| FR-14 | v0.2 | 显示一致性、显式冲突和证据空白 | 已完成 |
| FR-15 | v0.2 | FICI 逐行计算、分类与错误审计 | 已完成 |
| FR-16 | v0.2 | 生长曲线重复汇总和分组 AUC | 已完成 |
| FR-17 | v0.2 | 带证据、风险和下一步的 Markdown/JSON 报告 | 已完成 |
| FR-18 | v0.2 | 人工复核通过、要求修改或拒绝 | 已完成 |
| FR-19 | v0.2 | 本地保存并恢复任务、工具调用和失败记录 | 已完成 |
| FR-20 | v0.2 | 五步 Streamlit 工作台 | 已完成 |

## 8. 数据与引用契约

- PubMed 文献以 PMID 为主要身份，同时保留 DOI、摘要片段和来源 URL；
- 用户导入文献使用稳定来源 ID，可有 DOI，但 PMID 缺失时必须保持为空；
- 文献结论至少关联 PMID、DOI、来源 URL 或原文片段中的一项；
- 实验分析保留 CSV 原始行、行号、有效性和错误信息；进入报告时必须同时记录文件名、SHA-256、数据行与计算过程；
- 无效或部分无效 CSV 不进入结论和建议，并在报告中形成明确证据空白；
- 合成演示数据必须带 `synthetic_demo` 标记，并在报告中声明不可作为科研证据；
- 缺失字段显示为空或“未报告”，不能由规则或模型补造；
- 用户导入题录、实验 CSV 和 `.workbench` 运行记录仅在本机处理，不提交仓库。

## 9. 当前验收证据

- 全量自动化测试：`69 passed`；
- 继承自 v0.1 的定向评测：`30/30`；
- 真实默认场景执行 3 轮 NCBI PubMed 检索，去重后返回 5 个唯一 PMID；
- RIS、EndNote、RefWorks 格式识别、字段映射、去重和异常输入均有自动测试；
- FICI 覆盖协同、相加、无关、拮抗边界及非法 MIC；
- 生长曲线覆盖重复汇总、标准差、梯形 AUC 和坏行审计；
- 决策报告无法在没有可追溯来源时生成；
- 运行快照可保存、加载并保留失败、重试关系和人工复核；
- 五步 UI 已接通问题、文献、实验、报告和运行恢复。

真实 PubMed 返回结果会随外部数据库更新而变化；以上 5 个唯一 PMID 是当前验收记录，不是固定产品承诺。测试和定向评测是工程证据，不代表通用科研准确率。

## 10. 风险与下一步

- 规则提取无法覆盖所有摘要表达方式；
- RIS、EndNote、RefWorks 的平台导出变体可能需要补充兼容样本；
- 摘要和题录常缺少剂量、时间、对照或完整实验方法；
- 当前冲突检测只识别显式效应方向，不能替代人工语义复核；
- FICI 阈值分类和生长曲线 AUC 都是描述性结果；
- LetPub 不是官方 API，分区信息需要正式复核；
- 当前本地快照恢复以完整运行 ID 作为访问凭证，仅支持可信单用户本机模式；共享部署前必须增加账号和对象级授权；
- 下一步应使用真实、可公开的协同实验案例做人工标注和端到端复核，再决定是否增加全文解析或 LLM Provider。
