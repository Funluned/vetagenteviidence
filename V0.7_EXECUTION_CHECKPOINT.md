# VetResearch Workbench v0.7 执行检查点

## 当前状态

- 当前记录日期：2026-08-02
- 当前执行线：阶段 4E／阶段 6｜发布收口两次全 27 题真实运行已完成。第一次
  单／双为 `26/27`、`25/27`，暴露注入证据准入和 Reviewer 截断缺口；两项
  零费用修复后第二次单／双为 `23/27`、`24/27`。两次是独立报告，不拼分。
  v0.7.0 文档、版本与两份脱敏报告已提交并推送，GitHub 仓库已更名为
  `Funluned/vetresearch-workbench`；Docker 远端 CI、PR 合并、Tag 和 Release
  尚未完成
- 当前分支：`codex/vetresearch-workbench`
- 发布收口提交：`de47a95`（已推送）
- 审计执行基线：`7a1823035ac20c05510ae6de3e8c797d55532dd3`
- 阶段 1 代码收口提交：`003e2e02b7b0f64cf60bf1abb06c71f47a6e4a38`
- 远端默认分支：`main`
- 远端 `main`：`e8bbd1afb9310714a7c3c9015b99919e2c284c29`
- 阶段 1 功能代码已推送至：`003e2e02b7b0f64cf60bf1abb06c71f47a6e4a38`（后续仅追加本阶段收尾记录）
- `7a18230`、`b8dd513` 与 `003e2e0` 均已推送至远端开发分支。
- 已跟踪文件在阶段 1 开始前无未提交修改；未跟踪的 `.codex-run-venv/` 是本地虚拟环境，本阶段只加入忽略规则，不删除。

## 已确认的范围决定

- 保留现有规则系统、证据分层、PubMed、FICI、生长曲线、数据库、对接与 MD 技术烟测能力，不推倒重写。
- v0.7 主线是评测驱动的 LLM、RAG、单 Agent 与双 Agent 证据审查实验。
- 阶段 1 只处理跨平台修复、三平台 CI 和相应执行记录，当时不提前加入 LLM、
  RAG 或 Agent 代码。
- 孙奇本人从零安装、讲解、修改和排错验收已延期；它不作为当前代码与 GitHub 收口的放行门禁，待 GitHub 工程状态稳定后由孙奇单独完成。
- 大壮已确认阶段 1 提交可推送，并授权在同一修复范围内持续修复和复跑 CI；创建或合并 PR、发布 Release、修改仓库设置仍须另行确认。
- 任何付费模型调用、敏感数据导入或许可受限材料处理仍须事前确认；阶段 1 不需要这些操作。
- 阶段 2 只建立版本化评测集、评分方法和 `rules_v1` 快照；不接入或调用 LLM，
  不实现 RAG、单 Agent 或双 Agent，也不访问实时 PubMed／LetPub。
- 阶段 3A 先做无需 API Key、无需模型订阅的离线核心；免费基本路径不依赖
  DeepSeek、Codex／MCP 或任何本地模型。
- 产品不再自带本地语义模型，也不另设面向普通用户的“付费增强版”。DeepSeek
  只作为开发者隔离评测 Provider，用于证明项目真实开发过 LLM Agent；用户使用
  免费工作台时无需配置它。
- 真实 DeepSeek 调用必须先说明模型、题数、调用上限、预计费用、硬上限和发送
  内容，再由大壮单独确认。推送 GitHub 仍需另行确认。

## 阶段 0 审计结果

### Git 与远端（审计时快照）

- 审计时开发分支相对远端开发分支 `ahead 1 / behind 0`。
- 审计时远端开发分支相对远端 `main` 多 2 个提交；本地开发分支相对远端 `main` 多 3 个提交。
- 本地 `main` 是旧引用，比远端 `main` 落后 12 个提交；阶段 1 不以本地 `main` 为基线。
- 审计时 GitHub 没有 Actions workflow。

### 环境与运行

- Python：`3.11.15`
- `python -m pip check`：`No broken requirements found`
- Streamlit 健康检查：`200 / ok`
- Streamlit 首页：`200`
- 本机未安装 Docker；Docker 构建不属于阶段 1 的修改范围。

### 测试

- 四个已知跨平台目标测试在当前 Windows 环境中：`4 passed in 0.88s`
- 当前 Windows 全量测试：`428 passed in 24.85s`
- Windows 通过不能证明 macOS/Linux 已通过。代码审计确认前三个 POSIX 失败来自测试夹具未设置执行位；GeneCards 失败来自宿主平台相关的路径 basename 处理。

### 当前 AI 能力事实

- 免费工作台仍是透明规则和本地关键词检索，不调用生成模型；当前哈希向量基线
  也不是语义 Embedding，不能宣传为语义 RAG 增益。
- 隔离评测层已有真实 DeepSeek V4 Provider、有限状态 LLM Research Agent 和
  只读 Evidence Reviewer；历史正式报告的 v1 scorer 记录为规则 `20/27`、真实
  单 Agent `24/27`、真实双 Agent `24/27`，修正 scorer 离线重算为单／双均
  `22/27`。这只证明冻结合成场景中的真实调用与工程表现，不能声称完整模型效果、
  科研正确性或临床有效性已验证。
- 27 题 Fake 合同烟测只验证编排、安全、引用、预算和评测链；Fake 不是 LLM。

## 阶段 4E／阶段 6 发布收口两次全量与确定性修复

- 第一次报告：
  `data/eval/v0.7/results/agent_deepseek_deepseek-v4-pro_20260802T082341665388Z.json`；
  输入／结果 SHA-256 分别为
  `583ab368d37dfaecd61be6cc64d9641b0befcdbfbc23d453b64abc41282f8f3f`、
  `93a1fc3c5f2fba419ea8e8c7c84f813df00a94ebfc17bffc46f0ddf82f9ab533`。
- 第一次单／双 `26/27`、`25/27`；70 次 HTTP 200，67 `stop`、3 `length`；
  精确费用 `¥0.4456658`。`INJ-02` 暴露控制文本误成为直接证据，`DIR-01`
  暴露 Reviewer 2048 Token 截断后缺少有界恢复。报告原样保留。
- 两项零费用修复：控制语言命中的标题／摘要仍保留审计内容但强制
  `out_of_scope`；Reviewer 首次 `truncated_output` 只能消耗唯一 retry，
  从 2048 提高到 4096，第二次失败继续转人工。Fake 单／双更新为 `25/27`，
  稳定 SHA-256 为
  `053f2d35f64d74f46ec83f4af96f697f2e1e424ec7eb0221805c67a07cd18d90`；
  Windows 全量 `648 passed, 1 skipped`，三套基线匹配。
- 第二次报告：
  `data/eval/v0.7/results/agent_deepseek_deepseek-v4-pro_20260802T085911981817Z.json`；
  输入／结果 SHA-256 分别为
  `901b7d507c483c38cc60d2ac33476dda9908f178ace936cf7676a960c87f92cb`、
  `3962523562e38944eb09b6d997ac96b055377d3184fc05a7be0a298dd83568c0`。
- 第二次单 Agent `23/27`，失败 `CON-02`、`CIT-01`、`TOOL-02`、`TOOL-03`；
  双 Agent `24/27`，失败 `CON-02`、`TOOL-02`、`TOOL-03`。71 次 HTTP 200，
  69 `stop`、2 `length`，HTTP 自动重试 0；本次精确费用 `¥0.4318394`，
  两次累计 `¥0.8775052`。
- 第二次 input/output/reasoning Token 为 `64,663 / 55,318 / 49,388`；单／双
  Recall `.8/.8`、Citation Precision `.75/1.0`、Unsupported Claim Rate
  `.25/0`、Abstention Accuracy `.92/.96`、Task Completion `1/1`，中位耗时
  `9.75 s / 23.38 s`。
- `INJ-02`、`DIR-01` 单／双均通过。`TOOL-02` Reviewer 截断后真实完成
  2048→4096 恢复，但检索不完整，因此仍安全拒答。两项修复真实生效不等于
  模型稳定或双 Agent 全面优于单 Agent。
- 阶段 6 当前只完成本地版本、许可证、Changelog 和文档材料准备；Docker 实际
  验证、远端 CI、提交、推送、合并、Tag、Release 仍待后续门禁。阶段 5 本人
  安装、讲解、修改和排错验收继续延期。

## 阶段 4D 五题真实复测与第二轮零费用修复

- 经 `¥5` 硬上限单独确认后，DeepSeek V4 Pro 定向复测 `DIR-01`、`CON-01`、
  `CON-02`、`HIT-03`、`TOOL-03`：16 次 HTTP 200 调用，实耗 `¥0.1118206`，
  单／双 Agent 均为 `2/5`；`CON-01`、`CON-02` 通过。
- 真实报告为
  `data/eval/v0.7/results/agent_deepseek_deepseek-v4-pro_20260802T062927153218Z.json`，
  报告内部结果 SHA-256 为
  `7f2d37c9eb6f7b916174910ae27d1e4349740ba73704c43a3b60e3a286c7df1e`。
- `DIR-01` 暴露草稿 JSON 契约不够明确；`HIT-03` 暴露 contextual 证据仍可进入
  结论；`TOOL-03` 同时暴露检索词缺少研究实体和 2048 输出 Token 被 reasoning
  耗尽。Reviewer 额外花费 `¥0.020025`，没有改变本轮得分。
- 第二轮修复未读取 Key、未联网、未产生模型费用：草稿修复提示加入精确 schema
  契约；证据账本加入 direct/contextual/out-of-scope/validated 分级和确定性门禁；
  检索 query 确定性补齐 Population、Intervention、Comparator、Outcome；仅草稿
  首次截断可消耗原有一次修复机会，将输出上限由 2048 有界提高到 4096。
- 新 Fake 基线为规则 `20/27`、单／双 Agent `24/27`，失败仅为 `CIT-01`、
  `CIT-02`、`INJ-02`；模型调用、网络、Token 与费用均为 0，结果 SHA-256 为
  `d94a1a87a869d72b342897125c54bb1c5fa73a1e7a0874ad39a39e6738aed76c`。
- 经新的 `¥5` 上限确认复测 `DIR-01`、`HIT-03`、`TOOL-03`；6 次 HTTP 尝试中
  4 次为 200、2 次为 400，精确费用 `¥0.0270374`，单／双均为 `1/3`。报告内部
  结果 SHA-256 为
  `97432480e00d9df1117e390fe20a642fbff01971b3e2550b19538953572df950`。
- `HIT-03` 已通过。另两题的失败请求已离线重建且哈希与报告匹配，根因为 drafting
  提示缺少 DeepSeek JSON Output 强制要求的字面量 `json`；现已补回并加入回归
  断言，但尚未再次真实调用。
- Windows 全量验证为 `642 passed, 1 skipped`。最终两题复测已另行取得下面记录的
  `¥1` 明确付费确认。
- 经 `¥1` 上限确认完成最终两题复测：7 次 HTTP 200 调用精确记账 `¥0.0634410`，
  单／双 Agent 均为 `2/2`，Reviewer 均批准。报告内部结果 SHA-256 为
  `105b4d8c85a0d61dc9328c683c09fcc3955b73be4213c7e7e21410420ccf8242`。
- `DIR-01` 首次草稿成功；`TOOL-03` 首次草稿达到 2048 Token 截断后，只使用
  现有唯一 retry，以 4096 上限恢复成功。该结果不与不同代码版本拼成全 27 题成绩。
- 功能提交 `4401e4910fa86243dcc00e358bc6bf1ac941865b` 已推送；GitHub Actions
  [`30738353459`](https://github.com/Funluned/vetresearch-workbench/actions/runs/30738353459)
  三平台全绿：Ubuntu `640 passed, 3 skipped`、Windows `639 passed, 4 skipped`、
  macOS `640 passed, 3 skipped`。

## 阶段 4C Agent 零费用工程修复

- 历史正式报告保持原样：v1 scorer 记录规则 `20/27`、真实单 Agent `24/27`、
  真实双 Agent `24/27`，内容完整性 SHA-256 仍为
  `1d20b4c079f3b51d8eb3d3668364576302882c0ffb007b8be0fb4f3b70a62727`。
- 修正 scorer 对同一历史 actual 的离线重算为单／双 Agent 均 `22/27`，失败题为
  `DIR-01`、`CON-01`、`CON-02`、`HIT-03`、`TOOL-03`。旧 JSON 未覆盖，模型未
  重新调用。
- Task Completion 修正为单／双均 `24/27`，而 v1 记录为 `26/27` 与 `27/27`；
  `human_review_required` 只记安全交接，不升级为 Research completion。
- 冲突评分现在要求有效的两侧引用、各自方向与最终开放冲突；FICI 冲突必须由
  结构化分析事实支持。
- DeepSeek timeout 默认 `120` 秒，受限可配置范围为 `30—300` 秒，并由 CLI
  显式审计；Provider HTTP retry 仍为 0。
- 新报告的调用 ID 全局唯一；账本越界引用最多触发一次仅可使用当前 ledger 的
  bounded redraft；`report.build` 作为独立 trusted 工具授权。
- 新 Fake 基线仍为规则 `20/27`、单／双 `17/27`，冲突场景 `3/3`；模型调用、
  网络、Token 与费用均为 0，结果 SHA-256 为
  `40e1fb1a5f5f6a71a4b02bd2864ccd4b6a7ef0b74342b61d001f63b99a768a89`。
- 相关专项 `100 passed`；全量 `628 passed, 1 skipped`；3 份历史真实报告均可读取
  且文件未改，刷新后的 Fake 基线也可读取并复跑匹配。
- 功能提交 `85fdf7b` 已推送；远端 CI
  [`30733414142`](https://github.com/Funluned/vetresearch-workbench/actions/runs/30733414142)
  三平台全绿：Ubuntu `626 passed, 3 skipped`、Windows `625 passed, 4 skipped`、
  macOS `626 passed, 3 skipped`。修复后没有真实复测。

## 阶段 3B／4A 本地交付与门禁

- Provider 固定官方 HTTPS 端点，只在真实生成时读取环境变量 Key；默认预算为
  0。一次 CLI 运行只有一个共享人民币上限，覆盖 Research、Reviewer 和 Revision。
- Research 每题最多 3 个计划项、4 次工具调用、2 次正常模型调用和 1 次修复；
  Reviewer 最多 2 次审查、1 次定向修订和 1 次格式修复。
- Provider 可见输入只含问题、科研范围、中性来源别名和实际工具证据；gold、
  评分词、case 分类、`SYN-*` 与原始测试 URL 不进入提示词。
- 单／双 Agent 共用同一次 Research 结果与同一证据账本；Reviewer 不能重搜，
  未批准时只公开安全拒答或人工复核状态。
- 固化 `agent_fake_v1.json`：规则 `20/27`、Fake 单 Agent `17/27`、Fake 双 Agent
  `17/27`，该 Fake 运行自身的真实模型／HTTP／Token／费用为 `0 / 0 / 0 / ¥0`。
  该成绩不是 DeepSeek 或科研质量成绩。
- CLI 默认 Fake；真实模式必须显式选题、`--confirm-paid-run`、正数
  `--max-cost-cny` 和 Key。dry-run 不读取 Key、不构造 Provider、不联网。
- Agent 专项 `76 passed`；全量收集 `603` 项，执行 `602 passed, 1 skipped`；
  三套 v0.7 基线、编译、依赖和差异检查通过。最终只读安全审查没有剩余高、
  中等级问题。
- 经大壮单独确认后，V4 Pro 五题首测已执行：14 次调用、`¥0.0488516`，单／双
  `4/5`；`TOOL-02` 定向复测 3 次调用、`¥0.0276604`，单／双均通过。累计
  17 次调用、`¥0.0765120`。其余四题未在修复版上重跑，不拼成正式 `5/5`。

## 阶段 4B 全 27 题正式工程评测

- 报告：
  `data/eval/v0.7/results/agent_deepseek_deepseek-v4-pro_20260802T030725971226Z.json`；
  内容完整性 SHA-256：
  `1d20b4c079f3b51d8eb3d3668364576302882c0ffb007b8be0fb4f3b70a62727`。
- 27 个唯一 ID 与冻结输入顺序一致，九类各 3；Pydantic、报告自身哈希、输入与
  规则基线哈希、共享 Research 和 `retrieval_reused=true` 全部通过。
- 规则／单 Agent／双 Agent 分别 `20/27`、`24/27`、`24/27`。单／双核心质量
  指标相同：Recall `5/5`、Citation Precision `2/2`、Unsupported Claim `0/2`、
  Abstention `23/25`；Task Completion 为 `26/27` 与 `27/27`。
- 双 Agent 增量为 `¥0.2586836` 和约 `19.19 s` 单题中位耗时，三项任务书核心
  质量指标均无改善；所谓一题 Task Completion 提升只是失败态转人工复核，
  Reviewer 在该题没有调用。因此双 Agent 只保留实验模式。
- 全批 83 次请求：74 次可用输出、1 次空输出、8 次网络超时。总保守记账
  `¥0.5482916`，由有回执的 `¥0.3619676` 和无响应最坏预留 `¥0.186324` 组成；
  不把后者冒充精确账单。
- 失败为 `DIR-01` 账本外引用、`CON-01` 冲突误拒答并叠加 Reviewer 超时、
  `TOOL-03` 漏报告步骤并连续草稿超时；失败报告不删除、不覆盖、不择优重跑。
- 验收新增下一版工程项：冲突 scorer 当前不核对两侧来源／方向／开放冲突，
  `request_id` 仅题内唯一（83 次调用只有 59 个全局唯一值），30 秒 Provider
  超时不可配置。先修代码和测试，再另行决定是否小额定向复测。
- Key、Authorization／Bearer、私有路径、`SYN-*` 和 `example.invalid` 扫描为
  0；真实 Key 没有写入结果、跟踪文件或父进程环境。
- Agent 专项 `125 passed`；全量 `602 passed, 1 skipped`；规则、RAG、Fake
  Agent 三套基线、Python 编译、`pip check` 与 Git 差异检查均通过。

## 阶段 1 修改范围

1. 让外部工具测试夹具在 POSIX 上为假可执行文件设置执行位，同时保留生产端“不可执行文件必须拒绝”的安全检查。
2. 建立与宿主平台无关的安全文件名归一化，兼容 Windows、POSIX、UNC 和混合分隔符，不把目录写入 provenance。
3. 新增 Ubuntu、Windows、macOS 与 Python 3.11 的 GitHub Actions CI，执行依赖安装、Python 编译检查和全量 pytest。
4. 将 `.codex-run-venv/` 加入 Git 忽略规则，但不删除本地目录。
5. 将 SHA-256 固定的 vendored 3Dmol 资产标记为 `-text`，阻止 Windows 自动换行转换改变检出字节。

## 阶段 1 放行门禁

- [x] 四个目标测试通过。
- [x] 新增路径与执行权限回归测试通过。
- [x] Windows 本地全量测试无新增失败。
- [x] Python 编译检查、依赖检查和 Git 差异检查通过。
- [x] 推送后 Ubuntu、Windows、macOS CI 全绿。
- [x] 不使用 `skip`、`xfail`、弱化断言或移除生产安全检查制造通过。

## 阶段 1 本地验证结果

- 原四个目标测试：`4 passed`
- 对接可视化与受限数据库导入专项：`49 passed, 1 skipped`
- 阶段 1 最终 Windows 全量测试：`441 passed, 1 skipped in 23.66s`
- 固定哈希资产与项目制品回归：`14 passed`
- Windows 上跳过的是新增的 POSIX 不可执行文件负例；该测试会在 Ubuntu 和 macOS 执行，不用于隐藏原四个失败。
- `python -m pip check`：`No broken requirements found`
- `python -m compileall -q app.py scripts src tests`：通过
- GitHub Actions YAML lint：通过
- `git diff --check`：通过
- CI 使用官方 `actions/checkout@v7` 与 `actions/setup-python@v7`；审计时最新正式版本分别为 `v7.0.1` 与 `v7.0.0`。

## 阶段 1 远端 CI 结果

- 首次运行 [`30695157489`](https://github.com/Funluned/vetresearch-workbench/actions/runs/30695157489) 中 Ubuntu 与 macOS 通过，Windows 为 `1 failed, 436 passed, 4 skipped`。
- 唯一失败是 vendored `3Dmol.es6-min.js` 的固定 SHA-256 校验。干净 Windows clone 在 `core.autocrlf=true` 下把文件中唯一的 LF 转成 CRLF，使工作树哈希从预期的 `95513f64…6d427` 变为 `38630fd5…9011`；业务逻辑与资产来源未变化。
- 提交 `003e2e0 固定哈希资产的跨平台检出字节` 新增精确 `.gitattributes` 规则和回归测试。修复后的干净 Windows clone 保持 `w/lf attr/-text`，SHA-256 与仓库固定值一致。
- 复跑 [`30695477003`](https://github.com/Funluned/vetresearch-workbench/actions/runs/30695477003) 全绿：Ubuntu `439 passed, 3 skipped`，Windows `438 passed, 4 skipped`，macOS `439 passed, 3 skipped`；三者的安装、编译和全量 pytest 步骤均通过。
- CI 跳过项来自既有的平台或可选能力条件；本阶段没有新增 `xfail`，也没有通过弱化断言、删除测试或移除生产安全检查制造通过。

## 阶段 2 评测集与规则基线

### 交付范围

- 新建 `data/eval/v0.7/cases.json` 与 `expected.json`：固定版本 `v0.7.0`，
  27 个完全合成、离线场景，任务书九类边界各 3 题；输入和金标准分离。
- 所有文献式标识使用 `SYN-*`，来源 URL 只使用 `example.invalid`；没有真实
  PMID、DOI、论文内容、用户数据或科研事实。
- 新建独立运行器和快照，不替换旧版 30 条实时单查询字段回归；两套评测的
  用途和成绩不得互相替代。
- 固定 Retrieval Recall@K、Citation Precision、Unsupported Claim Rate、
  Abstention Accuracy、Task Completion Rate、Cost、Latency 七项指标；每项
  同时保存口径和适用边界。
- `rules_v1` 真实执行现有检索融合、规则提取、文献准入、实验分析、引用回答
  和任务状态路径；冻结 client 与离线分区 provider 保证不访问外部服务。
- 保存数据集 SHA-256、规则实现 SHA-256 和稳定结果 SHA-256；稳定哈希排除
  运行时间与本机延迟，`--check-baseline` 会重算已存快照，不能只比较旧哈希字段。

### 候选基线结果

- `20/27` 符合理想产品金标准，`7/27` 保留为规则能力差距；评测运行错误为 0。
- 七项指标：冻结回放 Recall@3 `3/5`，合成 claim-citation Precision `4/7`，
  Unsupported Claim `3/7`，Abstention `20/25`，Task Completion `26/27`，
  LLM 模型调用／Token／API 费用均为 0；Latency 仅保存本机单题中位数诊断值。
- 7 个差距是：中文“无乳链球菌”假否定导致拮抗方向漏识别；三个预造冲突
  EvidenceRecord 未被回答器二次核验；两条来源内容投毒被误准入；多路检索
  一路失败时未保留其他成功批次。
- 间接背景和“命中但不支持”的 5 题允许带背景引用地阻断目标结论，不再用
  “零引用＋固定短语”误判为产品失败；单次检索超时按现有任务边界记为受控失败。
- 金标准状态为 `engineering_gold_pending_domain_expert_review`；这表示工程口径
  已固化，但涉及科研语义的正式对外使用仍需领域人工复核。

### 阶段 2 门禁

- [x] 27 题、九类各 3、ID 唯一，输入与金标准一一对应。
- [x] 数据、预期结果、评分方法、边界和规则基线快照全部落盘。
- [x] 外部网络、模型、Token、API 费用与外部动作均为 0。
- [x] 规则失败如实保留；没有用 `skip`、`xfail` 或修改金标准制造全绿。
- [x] 快照内容、数据和实现哈希可以离线复跑校验。
- [x] 旧版 30 条评测保持不变。

### 阶段 2 本地与远端验证

- v0.7 专项、旧评测兼容和项目制品回归：`15 passed`。
- 最终 Windows 全量自动回归：`449 passed, 1 skipped in 24.64s`。
- 跳过项来自既有平台条件，不是阶段 2 新增的 `skip` 或 `xfail`。
- `python -m compileall -q app.py scripts src tests`：通过。
- `python -m pip check`：`No broken requirements found`。
- `scripts/run_v07_rule_baseline.py --check-baseline`：保存快照完整性、数据哈希、
  规则实现哈希和稳定结果哈希全部匹配。
- `git diff --check`：通过；仅有 Git 对普通文本下次检出换行形式的提示，
  没有空白错误，也不影响固定 `-text` 资产。
- 阶段提交 `b61d74254383496b15d02e5259243995ea94741f` 已按确认推送至
  `codex/vetresearch-workbench`；远端提交、树及父提交均与本地一致。
- GitHub Actions [`30698205011`](https://github.com/Funluned/vetresearch-workbench/actions/runs/30698205011)
  全绿：Ubuntu `447 passed, 3 skipped`，Windows `446 passed, 4 skipped`，
  macOS `447 passed, 3 skipped`。

## 当前停点

阶段 0—4 的历史实现、基线、定向复测和原始报告均保留。发布收口第一次全量
单／双为 `26/27`、`25/27`；两项零费用确定性修复后，Fake 单／双为 `25/27`，
Windows 全量为 `648 passed, 1 skipped`；第二次全量单／双为 `23/27`、`24/27`。
这些结果分别属于各自代码状态和独立报告，不合并为一个新分数。

当前停点是阶段 6 本地收口：第二次报告已经生成并校验，版本、许可证、Changelog
和求职材料仍在工作树中整理。Docker 实际构建与健康检查、远端 CI、提交、推送、
合并 main、v0.7.0 Tag 和 Release 均未在本轮写成完成。孙奇本人从零安装、讲解、
修改和排错验收继续按确认决定延期到 GitHub 工程稳定后。
