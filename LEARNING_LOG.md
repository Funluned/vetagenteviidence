# 学习与执行日志

## 2026-07-29

### 今天新增

- 建立 Python 隔离环境和项目目录。
- 确认 NCBI PubMed E-utilities 可访问。
- 完成真实文献检索与 Streamlit 页面。
- 用目标论文的 PMID、DOI 和摘要完成页面验收。
- 增加结构化证据模型、透明规则提取、带引用回答和导出功能。
- 真实摘要成功提取样本量、剂量、途径、时长和机制。
- 自动测试由 4 条增加到 9 条。
- 建立 30 条定向评测，覆盖检索、引用、结构化字段、缺失值和冲突保留。
- 增加 NCBI 临时失败重试、请求次数与 LLM 零成本记录。
- 完成 Dockerfile、PRD、架构、演示、简历证据、面试讲解和项目复盘。
- 最终回归：15 条自动测试和 30 条定向评测通过，依赖检查无损坏。
- 增加中科院 2025 与 JCR 2026 双分区展示，按 ISSN 优先匹配。
- 分区信息同步进入文献列表、证据表、Markdown 和 CSV。
- 分区数据采用可替换 CSV；默认只提供演示子集，未匹配期刊不猜测。
- 双分区功能回归后自动测试增加到 18 条。
- 将静态分区子集升级为 LetPub 动态查询：按 ISSN 定位期刊，分别解析中文页的中科院 2025 年 3 月升级版和英文页的 WOS JIF 分区。
- 增加 7 天 JSON 缓存、同批期刊去重、最多 4 路并发、过期缓存及本地 CSV 回退。
- JIF 结果保留 WOS 分类、SCIE 收录集和名次；界面、证据表及导出继续同时展示两套标准。
- 新增 LetPub 搜索页、分区页、缓存与 503 回退测试，自动测试增加到 21 条；定向评测仍为 30/30。
- 配置 GitHub 身份与远端，将完整项目上传至 `Funluned/vetagenteviidence` 的 `main` 分支。

### 已运行命令

```powershell
python --version
git --version
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m pytest
.\.venv\Scripts\python.exe -m streamlit run app.py
```

### 遇到的问题与修复

- 第一版默认检索词包含过多限定词，真实 PubMed 返回 0 条。
- 将默认词精简为 `quercetin Streptococcus agalactiae mastitis` 后返回 2 条，并命中目标论文。
- 这说明检索不是关键词越多越好；过度限定会降低召回率。
- 新模块加入后，运行中的 Streamlit 保留旧模块缓存并出现导入错误。
- 完整重启 Streamlit 后恢复，说明热重载不能替代所有依赖变更后的进程重启。
- 首次评测为 28/30；两条失败来自评测器把 `answer_contains` 错当成通用字段检查。
- 修复评测器分支顺序并增加回归测试后，定向评测为 30/30。
- LetPub 中文页的大类分区数字位于同一单元格的可见 `span` 中，不能假设“大类”和“分区”一定分行；解析器按实际结构拆分后，真实页面返回中科院大类 3 区和兽医学小类 3 区。
- LetPub 同页同时提供 JIF 与 JCI 分区；本项目固定读取 `Quartiles By JIF`，避免把两种指标混在一起。
- 当前网络中 GitHub CLI API 可访问，但 Git 原生 HTTPS 连接超时；首次上传改用 GitHub 官方 Git Data API，并用树哈希和提交哈希确保远端内容与本地一致。

### 当前理解重点

- API 请求：程序把检索词发送给 NCBI，NCBI 返回结构化的文献数据。
- PMID：PubMed 给每条文献分配的唯一编号，可用于追溯来源。
- 隔离环境：`.venv` 把本项目依赖与系统 Python 分开。
- 结构化提取：把摘要中明确报告的信息映射到固定字段，未报告内容保持为空。
- 引用回答：结论旁边必须保留 PMID、DOI 或来源原句，不能只给无来源总结。
- 测试与评测不同：测试检查代码行为，评测检查当前任务样本上的结果质量与边界。
- Dockerfile 描述可复现环境，但只有实际构建和运行后才能声称 Docker 验证通过。

### 孙奇需要亲自完成

- [ ] 启动 Streamlit 页面。
- [ ] 输入一个检索词并打开一条 PMID 链接。
- [ ] 用自己的话解释“检索词 → NCBI API → 文献列表”的数据流。

### 下一步唯一重点

由孙奇配置 GitHub、验证 Docker、录制演示视频并完成一次亲自修改。

## 2026-07-29：VetResearch Workbench 增量

### 为什么升级

- v0.1 已证明“检索—提取—引用—导出”可运行，但仍停留在查论文和整理摘要。
- v0.2 把科研问题、文献来源、实验条件、原始 CSV、决策报告和人工复核串成一个可审计闭环。
- 第一版只覆盖“候选药物与抗生素对目标病原菌是否值得继续验证协同作用”，避免把一个已验证的小闭环包装成通用科研智能体。

### 关键取舍

- PubMed 证据继续使用 PMID；RIS/EndNote/RefWorks 导入记录不强制 PMID，改用稳定来源 ID、DOI、来源 URL 和原文片段。
- 首版 Agent 能力体现为显式顺序工作流、任务状态、工具调用、失败/重试和人工复核，不引入多 Agent 框架、FastAPI、数据库或向量检索。
- FICI 只做公式、固定阈值分类和逐行校验；生长曲线只做重复汇总、标准差和梯形 AUC，不自动做显著性检验。
- 知网只支持用户合法导出文件的本地导入，不自动抓取或绕过权限。
- 合成题录和 CSV 只用于流程验收；报告会明确标为不可作为科研证据。

### 实际验证

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe -m streamlit run app.py --server.address 127.0.0.1 --server.port 8501
```

- 全量自动测试：`72 passed`，v0.1 原有测试未回退。
- 真实三轮 NCBI 检索分别得到 7、8、0 条；公平合并并去重后保留 8 个 PMID，前两轮各贡献 4 条，证明自动多检索不是静态演示。
- 浏览器完成问题拆解、合成 RIS 导入、条件矩阵、FICI、生长曲线、报告、人工复核和历史恢复。

### 遇到的问题与修复

- Streamlit 多次完整重启后仍显示旧导入符号，最终发现 8501 同时残留两套本项目父子进程；只停止启动器并不会停止 `uv` 管理的实际解释器。
- 核对命令行和端口归属后，只终止已确认属于本项目的四个旧进程并启动唯一服务，旧缓存问题消失。
- 浏览器终验发现 Windows 文件监视器会短暂占用新写入的快照，导致原子替换偶发 `WinError 5`；改用唯一临时文件并对 `PermissionError` 做有限指数退避后，页面与磁盘实测均成功落盘。
- 为保持 v0.1 底座隔离，用户导入题录的字段提取最终放入独立 `imported_extraction.py`，不再改动原 PubMed 提取行为。
- 原检索式把候选药物、对照药物和病原体全部强制 AND，容易零召回；改为分别搜索候选干预、对照干预和联合/协同证据。
- 多轮检索原先按查询顺序装入结果再全局截断，第一轮可能占满名额；现在保留各查询内部相关性顺序并轮询取新 PMID，跨查询重复不消耗该轮机会，空查询或耗尽后由其他查询回填。
- 合成演示题录一度进入报告结论；现在该题录被排除，合成 CSV 结论明确标注“不可作为科研证据”。
- 最终复审发现任意来源 ID、无效 CSV 旧评估和复核版本绑定的追溯风险；现已用严格来源类型、输入 SHA-256 清单、评估重算、报告内容哈希与复核快照消除。
- 历史快照升级为 schema v2；缺少输入哈希的旧分析不会被伪造补齐，而是安全失效并保留迁移事件。

### 下一步唯一重点

孙奇亲自从零跑通并讲解固定协同研究案例，再根据真实科研数据决定下一类实验分析，而不是继续扩展未验证功能。此前“由孙奇配置 GitHub”的旧下一步已完成并由本阶段取代。
